import { AuthenticatorInterceptor } from './authenticator.interceptor';

describe('AuthenticatorInterceptor', () => {
  it('should be defined', () => {
    expect(new AuthenticatorInterceptor()).toBeDefined();
  });
});
